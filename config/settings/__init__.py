"""Pacote de settings modular.

Selecione qual módulo carregar via `DJANGO_SETTINGS_MODULE`:

- `config.settings.dev`    — desenvolvimento local
- `config.settings.test`   — execução de pytest
- `config.settings.prod`   — produção (Railway)

Não importe deste `__init__` — ele é vazio de propósito.
"""
