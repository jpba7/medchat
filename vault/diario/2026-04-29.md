---
title: 2026-04-29
type: diario
tags: [captura-retroativa, item-7-fase-1]
---

# Diário — 2026-04-29

## O que rodou hoje

- Sessão `local_affcf7df` (na real arquivo `~/.claude/projects/C--Repos-medchat/d2e519b2-fff0-47f6-9da6-008725e5211f.jsonl`, slug `proxima-frente-ent-o-snoopy-horizon`) tinha rodado **antes** de o `vault/` existir e o `/capture` virar slash command.
- Decidi fazer **captura retroativa** lendo o transcript da sessão e identificando candidatos vault-worthy.
- Como `mcp__ccd_session_mgmt__search_session_transcripts` não roda em auto mode (requer interação humana), a alternativa foi ler o `.jsonl` direto: extrair mensagens user reais (filtrando `tool_result`), pegar as respostas mais longas do assistant, identificar conceitos.

## Captura realizada

A sessão entregou **12 commits** fechando o Item 7 da Fase 1 em 4 "ondas":

| Onda | Commits | Modelos |
|---|---|---|
| 1 — cadastro | `bef5de0`, `0b11361`, `52bf9b8`, `118c015`, `82cc114`, `b8e6418` | `ClinicaCanal`, `ClinicaPolitica`, `Paciente`, `Especialidade`, `Medico`, `Convenio`, `MedicoConvenio`, `MedicoDisponibilidade` |
| 2 — agendamento | `a9bcb07` (+ `3eaeb52` ruff format) | `Agendamento` com `EXCLUDE USING GIST` |
| 3 — conversação | `b69eeae` | `Conversa`, `Mensagem`, `Handoff` |
| 4 — envio + log | `00f3287` | `Outbox` (em `apps.channels`), `EventoBot` (em `apps.observability`) |

Resultado da captura: **11 notas novas + 4 updates** no vault.

### Aprendizados (3)

- [[aprendizados/snapshotter-overlayfs-corrompido]] — `wsl --shutdown` brusco corrompe snapshotter overlayfs do Docker Desktop. Sintomas confundem (exec format error, 502 Bad Gateway). Solução: Clean/Purge data via UI.
- [[aprendizados/medchat-superuser-bypassrls]] — user `medchat` do compose é SUPERUSER; RLS é silenciosamente ignorada em runtime. Pendência hardening prod.
- [[aprendizados/grant-faltava-na-migration]] — roles `app_readwrite`/`app_jobs` sem GRANT nas tabelas. Resolvido em `core/0002`.

### Decisão (1)

- [[decisoes/clinica-id-desnormalizado-vs-fk]] — through-tables (`MedicoConvenio`, `Mensagem`, etc.) ganham `clinica_id` próprio + RLS própria, em vez de depender de JOIN. Defesa em profundidade.

### Conceitos (3)

- [[conceitos-ai/exclude-using-gist]] — anti-overlap de agendamento atômico no banco
- [[conceitos-ai/outbox-pattern]] — desacoplar request de chamada externa
- [[conceitos-ai/idempotencia-via-unique-parcial]] — `UNIQUE ... WHERE` parcial pra dedup de webhook

### Entidades (5)

- [[entidades/paciente]]
- [[entidades/catalog]] (5 modelos)
- [[entidades/agendamento]]
- [[entidades/conversations]] (3 modelos)
- [[entidades/outbox]] (Outbox + EventoBot)

### Updates (4)

- [[integracoes/evolution-api]] — adicionada referência ao constraint parcial e link pra outbox-pattern
- [[fluxos/agendar-consulta]] — saiu do skeleton, estados preenchidos com nomes reais
- [[MOC]] — listadas todas as notas novas
- Este diário

## Decisões da sessão atual (não da capturada)

- Captura retroativa via leitura direta do `.jsonl` é viável quando `search_session_transcripts` não tá disponível.
- Não dá pra confiar 100% no `list_sessions` porque o `sessionId` retornado (`local_affcf7df`) não bate com o nome do arquivo no disco (`d2e519b2`). Heurística usada: comparar `lastActivityAt` com `mtime` do arquivo `.jsonl` no path do projeto.

## Vault candidates sinalizados (nesta sessão atual)

(Nenhum — sessão atual é puro mecânico de captura. Os candidatos listados acima são da sessão `d2e519b2`.)

## Próximo passo

- Continuar Item 7 → próximas frentes: `config/celery.py` (item 9), `config/api.py` + `/health` (item 8), webhook handler.
- Quando essas frentes ganharem código, adicionar em [[entidades/outbox]] o detalhe do `send_outbox` task, e em [[fluxos/agendar-consulta]] os passos concretos do handler de webhook.

## Notas tocadas hoje

- Criadas: `aprendizados/snapshotter-overlayfs-corrompido`, `aprendizados/medchat-superuser-bypassrls`, `aprendizados/grant-faltava-na-migration`, `decisoes/clinica-id-desnormalizado-vs-fk`, `conceitos-ai/exclude-using-gist`, `conceitos-ai/outbox-pattern`, `conceitos-ai/idempotencia-via-unique-parcial`, `entidades/paciente`, `entidades/catalog`, `entidades/agendamento`, `entidades/conversations`, `entidades/outbox`.
- Atualizadas: `integracoes/evolution-api`, `fluxos/agendar-consulta`, `MOC`.
