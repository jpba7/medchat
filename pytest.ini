[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
testpaths = tests
addopts = --tb=short --strict-markers -ra
markers =
    phase6: testes que dependem da WhatsApp Cloud API (skipados até a Fase 6)
    slow: testes lentos, opt-in (rodar com `-m slow`)
